> Experimental

This is a fork of GSD-opencode for minimal bash-only install and more fine-tuned agent permission.

For those who don't mind to approve and just want the slash commands and agents.

Note: still checking if this is doing any unwanted edits, use with care. call `bash restore.sh` for any issues.
